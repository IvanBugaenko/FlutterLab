import 'package:flutter/material.dart';

class SettingsTabContentWidget extends StatelessWidget {
  const SettingsTabContentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
